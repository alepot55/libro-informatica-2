# Config locale di Claude Code (machine-local, contiene path assoluti)
.claude/settings.local.json

# Buffer/handoff di sessione — locale, non sincronizzato
.remember/

# OS / editor
.DS_Store
*.swp
*~

# Build/artefatti (es. PDF generati)
/build/
*.aux
*.log
*.toc
*.out

# NOTA: memory/ è VERSIONATA di proposito (memoria sincronizzata su GitHub).
